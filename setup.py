from setuptools import setup, find_packages

setup(
    name='saudization_dashboard',
    version='0.1.0',
    description='Saudization (KSA) HR Analytics Dashboard for ERPNext',
    author='Your Company',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)
