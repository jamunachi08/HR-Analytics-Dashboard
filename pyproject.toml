[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "saudization_dashboard"
version = "0.0.1"
description = "Saudization HR Analytics Dashboard (Frappe app)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
